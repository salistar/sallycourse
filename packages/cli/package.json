{
  "name": "@sallycourse/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "sallycourse": "./dist/bin.js"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
